# Local env overrides (FL Sunbiz creds + per-machine config). .env.example
# committed as the template.
.env.local
.env

# Python venv + caches
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# uv lock is committed; uv state isn't
.uv-cache/
