.next
node_modules
.env
.env.local
.env.*
!.env.example
.vercel
dist
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.env*.local
*.tsbuildinfo

# Claude Code session artifacts (local-only)
.claude/

# Local enriched-lead CSVs — contain real contact info, keep out of git
lead_exports/

# Abandoned eviction-scraper discovery scratch — saved third-party court-site
# HTML/JSON that embed session/viewstate/telemetry tokens. Keep out of git
# (preserved on local disk; no preservation value on a pushed branch).
lead_ingestion/notes/
